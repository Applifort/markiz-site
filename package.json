{
  "name": "markiz-site",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4321",
    "build": "astro build",
    "preview": "astro build && astro preview --host 0.0.0.0 --port 4321"
  },
  "dependencies": {
    "astro": "^5.7.0"
  }
}
