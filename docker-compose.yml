services:
  dev:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - markiz_npm:/app/node_modules
    environment:
      NODE_ENV: development
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "4321"]

volumes:
  markiz_npm:
